const express = require('express');
const app = express();
app.get('*', (req, res) => {
    res.send('MINIMAL TEST APP - PORT 3000');
});
app.listen(3000, () => {
    console.log('Minimal Test App running on port 3000');
});
